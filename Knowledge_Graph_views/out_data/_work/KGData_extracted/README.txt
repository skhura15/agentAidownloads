KGData.zip sample contents:
- Scenario1.zip (rich sample)
- Scenario2.zip (small sample)

Each Scenario zip contains JSON node files.
Filenames start with a NodeType, e.g. Product_PROD-001.json.
Each JSON contains: id, type, label, properties, tags, relations.
