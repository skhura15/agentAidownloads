# Development Environment Configuration
# Overrides base config for development

azure_openai:
  model: "gpt-4"
  temperature: 0.7

logging:
  level: "DEBUG"
  
api:
  reload: true
  workers: 1

state:
  use_redis: false
